# Open Code

open code session list = shows sessions



## Inside opencode
1. /init = starts a new session
2. /list = shows all sessions
3. /attach [session_name] = attaches to a session
4. /detach = detaches from current session
5. /kill [session_name] = kills a session
6. /help = shows all commands

